# client

Vite + React + TypeScript app for ShiftFair. See the [root README](../README.md) for full documentation.

## Quick start

```bash
npm install
npm run dev      # dev server → http://localhost:5173
npm run build    # production build → dist/
```
